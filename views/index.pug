extends layout
include request

block content
  h3= search
  #feed
    each request in requests
      +request(request.title, request.time, request.distance,
               request.content, request.current, request.total)
